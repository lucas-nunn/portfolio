import styled from "styled-components";

// -------------------------
// Holds title buttons/links
// -------------------------
const TitleBar = styled.div`
    display: flex;
    justify-content: space-between;
    align-items: center;
`;

export default TitleBar;
